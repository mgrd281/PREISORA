name: radiusMeters
in: query
required: false
description: Search radius in integer meters (geo units are always meters - see CONVENTIONS.md).
schema:
  type: integer
  minimum: 1
  maximum: 50000
  default: 5000
