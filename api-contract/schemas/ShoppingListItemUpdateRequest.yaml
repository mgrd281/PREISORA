title: ShoppingListItemUpdateRequest
description: Partial update of a ShoppingListItem. Omitted fields stay unchanged.
type: object
properties:
  quantity:
    type: integer
    minimum: 1
  note:
    type:
      - string
      - 'null'
    maxLength: 500
    description: New note, or `null` to clear it.
